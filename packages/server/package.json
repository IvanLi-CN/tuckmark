{
  "name": "@tuckmark/server",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prebuild": "bun --cwd=../../packages/ipc run build && bun --cwd=../../plugins/inventory run build",
    "pretest": "bun --cwd=../../packages/ipc run build && bun --cwd=../../plugins/inventory run build",
    "pretypecheck": "bun --cwd=../../packages/ipc run build && bun --cwd=../../plugins/inventory run build",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch --tsconfig tsconfig.typecheck.json src/entry.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@tuckmark/core": "workspace:*",
    "@tuckmark/ipc": "workspace:*",
    "@tuckmark/inventory": "workspace:*",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6"
  }
}

{
  "version": 1,
  "default_branch": "main",
  "required_checks": ["Label Gate", "CI PR / Workspace", "CI PR / Web Demo"],
  "informational_checks": [
    "CI Main / Release Snapshot",
    "Pages Deploy",
    "Release Publish",
    "Notify Release Failure"
  ],
  "branch_protection": {
    "pr_only": true,
    "require_signed_commits": true
  },
  "expected_pr_workflows": ["label-gate.yml", "ci-pr.yml"]
}

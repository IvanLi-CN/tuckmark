pre-commit:
  commands:
    format:
      run: bun run format

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}

post-checkout:
  commands:
    sync-worktree:
      run: bun run scripts/sync-worktree-resources.ts
